import { FactorDirective } from './factor.directive';

describe('FactorDirective', () => {
  it('should create an instance', () => {
    const directive = new FactorDirective();
    expect(directive).toBeTruthy();
  });
});
